import React from "react";
import Input from "./Input";

export default () => (
  <section>
    <h1>Transfer</h1>

    <Input label="Wallet Identifier" />
    <Input label="Amount" />
  </section>
);
